// Class KNN
// This class is responsible for training and predicting using the K-Nearest Neighbors algorithm.
// It is a template class, so it can be used with different types of data.
//

class KNN
{
public:
    KNN(int k) : k(k)
    {
    }

    void train()
    {
    }

private:
    int k;
};